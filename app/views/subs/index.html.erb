<h1>All Subs</h1>
<ul>
  <% @subs.each do |topic| %>
  <li>
    <a href="<%= sub_url(topic.id) %>">
    <%= "#{topic.title} - #{topic.description}" %>
    </a>
  </li>
  <% end %>
</ul>

<a href="<%= new_sub_url %>">Create New Sub</a>
